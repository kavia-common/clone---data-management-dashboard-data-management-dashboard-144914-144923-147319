# Server
PORT=3001
HOST=0.0.0.0

# Public base URL exposed in Swagger and cross-service calls
BACKEND_PROTOCOL=http
BACKEND_HOST=localhost
BACKEND_PORT=3001
# Optional override
# BACKEND_BASE_URL=http://localhost:3001
