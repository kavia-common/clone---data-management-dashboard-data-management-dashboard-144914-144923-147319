<<<<<<< HEAD
# Backend configuration for mongodb_dashboard_backend

# Server
# Set PORT to 3001 to match deployment and docs URLs
=======
# Backend service env example

# MongoDB connection
MONGODB_URI=
# Optional explicit DB name (develop_kaviaroot / qa_kaviaroot / pre_prod__kaviaroot)
MONGODB_DB=
# Enable auto index creation in Mongoose (use with caution on large datasets)
MONGOOSE_AUTO_INDEX=false

# Server
>>>>>>> cga-cg9d6f2ee8
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

<<<<<<< HEAD
# CORS
# Change to your frontend origin if you want to restrict access
CORS_ORIGIN=*

# MongoDB
# IMPORTANT: Provide your MongoDB connection string here for your environment.
# If not set, the backend will use a built-in default (for development/demo).
# For production, ALWAYS set MONGODB_URI explicitly.
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster-host>/?retryWrites=true&w=majority&appName=<AppName>

# Optionally override the database name.
# If not provided, the app uses an environment-based fallback:
#  - development/dev/local/test -> develop_kaviaroot
#  - qa/staging                 -> qa_kaviaroot
#  - production/prod/beta/pre*  -> pre_prod__kaviaroot
# MONGODB_DB=develop_kaviaroot

# Mongoose options
# Enable if you want automatic index creation at startup (default false)
# MONGOOSE_AUTO_INDEX=true

# Optional verification (logs an estimated count for session_tracking on startup)
# VERIFY_COLLECTIONS=true

# Debug DB logs for list endpoints (prints filter, db, collection)
# DEBUG_DB_LOGS=true

# Optional: override session tracking collection name (to match case differences)
# SESSION_TRACKING_COLLECTION=session_tracking

# Swagger customization (optional)
# SWAGGER_TITLE=Dashboard API
# SWAGGER_VERSION=1.0.0
# SWAGGER_DESCRIPTION=REST API for Data Management Dashboard with MongoDB and Express
=======
# Swagger (optional)
SWAGGER_TITLE=Dashboard API
SWAGGER_VERSION=1.0.0
SWAGGER_DESCRIPTION=REST API for Data Management Dashboard with MongoDB and Express

# CORS
CORS_ORIGIN=
CORS_ORIGINS=
FRONTEND_ORIGIN=
REACT_APP_API_BASE_URL=
CORS_CREDENTIALS=false

# Rate limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200
>>>>>>> cga-cg9d6f2ee8
