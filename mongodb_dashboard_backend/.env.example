# Example environment file for the backend service.
# Copy this file to .env and adjust values as necessary.

# MongoDB connection string used by the backend (Express + Mongoose).
# Provided default points to the desired MongoDB Atlas cluster for real-time data.
MONGODB_URI=mongodb+srv://govindarajmalaiarasu_db_user:MGRaj2005@phaseonedata.qlyhyxu.mongodb.net/?retryWrites=true&w=majority&appName=PhaseOneData

# Optional server port (defaults to 3000)
# PORT=3001

# CORS settings (optional)
# CORS_ORIGIN=*
