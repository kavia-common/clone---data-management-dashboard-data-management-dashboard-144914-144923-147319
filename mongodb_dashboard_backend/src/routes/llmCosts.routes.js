/**
 * NOTICE: /api/llm-costs removal
 * All CRUD endpoints for LLM costs have been removed for the current application scope.
 * This router is intentionally left empty to prevent runtime import errors.
 */
'use strict';
const express = require('express');
const router = express.Router();

// No routes are registered intentionally. See documentation above.
module.exports = router;