'use strict';
const express = require('express');
const router = express.Router();

/**
 * User Analysis metrics routes have been removed.
 * This file remains as a no-op to avoid import errors if referenced.
 */
module.exports = router;
