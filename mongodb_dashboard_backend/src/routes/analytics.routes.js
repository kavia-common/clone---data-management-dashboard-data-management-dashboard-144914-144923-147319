'use strict';

const express = require('express');
const router = express.Router();

/**
 * Generic analytics routes removed (users new-over-time).
 * If LLM cost analytics are required, they are available via /api/costs or /api/llm-costs routes.
 */
module.exports = router;
