# Local development overrides to reduce CPU/memory usage from file system watchers.
# This file is not required in production.
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Keep host binding permissive for preview environments
HOST=0.0.0.0
PORT=3001
