# Frontend environment configuration for the dashboard (do not commit .env with secrets)

# Base URL for backend API (e.g., http://localhost:3001 or https://api.example.com)
REACT_APP_API_BASE_URL=http://localhost:3001

# Public URL of the frontend site (used for email redirect links if required)
REACT_APP_SITE_URL=http://localhost:3000
