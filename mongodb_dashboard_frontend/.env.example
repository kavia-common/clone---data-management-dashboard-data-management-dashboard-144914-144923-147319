# Frontend environment variables
# Base URL for backend API. Defaults to window.location.origin or http://localhost:3001 if not set.
REACT_APP_API_BASE=http://localhost:3001
# Optional site URL for auth redirects (not used directly here but reserved for future use)
REACT_APP_SITE_URL=http://localhost:3000
