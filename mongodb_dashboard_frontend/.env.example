# Frontend environment example

# Backend API base URL (include port); leave empty to use relative path and dev proxy if configured.
REACT_APP_API_BASE_URL=http://localhost:3001

# Optional API prefix used by backend (default /api)
REACT_APP_API_PREFIX=/api
