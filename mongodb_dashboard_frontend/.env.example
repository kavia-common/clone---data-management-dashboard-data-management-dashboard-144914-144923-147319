# Example environment file for the React frontend
# Backend root URL. For local development, the backend typically runs on port 3001.
# If left empty, the frontend will use same-origin for requests.
REACT_APP_API_BASE_URL=http://localhost:3001

# API prefix used by the backend. Defaults to "/api" if not provided.
REACT_APP_API_PREFIX=/api
