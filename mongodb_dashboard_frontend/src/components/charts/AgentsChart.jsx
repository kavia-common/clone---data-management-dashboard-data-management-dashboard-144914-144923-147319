/**
 * PUBLIC_INTERFACE
 * Wrapper to re-export the TypeScript AgentsChart so JS modules can import it
 * without specifying the .tsx extension.
 */
import TSXAgentsChart from "./AgentsChart.tsx";
export default TSXAgentsChart;
